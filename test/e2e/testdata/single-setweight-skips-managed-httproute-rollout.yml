apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: httproute-setweight-skips-managed-rollout
  namespace: default
spec:
  replicas: 2
  strategy:
    canary:
      canaryService: argo-rollouts-canary-service 
      stableService: argo-rollouts-stable-service 
      trafficRouting:
        managedRoutes:
        - name: canary-route1
        plugins:
          argoproj-labs/gatewayAPI:
            httpRoutes:
              - name: httproute-setweight-skips-managed
                useHeaderRoutes: true
            namespace: default
      steps:
      - setHeaderRoute:
          name: canary-route1
          match:
            - headerName: X-Test
              headerValue:
                exact: test
      - setWeight: 30
      - pause: {  }
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: rollouts-demo
  template:
    metadata:
      labels:
        app: rollouts-demo
    spec:
      containers:
        - name: rollouts-demo
          image: argoproj/rollouts-demo:red
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
